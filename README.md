
  # TemtseenPortal website creation

  This is a code bundle for TemtseenPortal website creation. The original project is available at https://www.figma.com/design/1ybpGry9I44FNfRhZ9NwoT/TemtseenPortal-website-creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  